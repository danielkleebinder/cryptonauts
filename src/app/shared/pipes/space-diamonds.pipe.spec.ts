import { SpaceDiamondsPipe } from './space-diamonds.pipe';

describe('SpaceDiamondsPipe', () => {
  it('create an instance', () => {
    const pipe = new SpaceDiamondsPipe();
    expect(pipe).toBeTruthy();
  });
});
